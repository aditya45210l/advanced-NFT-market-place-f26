import { Input } from "@/components/ui/input";
import FilterSidebar from "./Filter";

import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select";

const MarketNav = () => {
  return (
    <section className="grid grid-cols-3 items-center px-6 py-5">

      <div className="max-w-screen">
      </div>
    </section>
  );
};
export default MarketNav;
